
<h1> <%= Cocktail.find(params[:cocktail_id]).name.capitalize %> </h1>

<h2>Add a dose</h2>
<%= simple_form_for [@cocktail, @dose] do |f| %>
 <%= f.input :description %>
 <%= f.association :ingredient %>
 <%= f.submit "add a dose", class: "btn btn-primary" %>
<% end %>
